# API base URL including /api suffix (no trailing slash after /api)
# Local phone/PC via Vite proxy (recommended): /api
# Direct to API on this PC only: http://localhost:5172/api
# Staging/prod example: https://api.v2.cimmple.net/api
VITE_API_ROOT=/api
