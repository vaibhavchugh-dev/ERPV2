# API base URL including /api suffix (no trailing slash)
# Development: /api  (Vite proxy; see .env.development)
# Production:  https://api.v2.cimmple.net/api  (see .env.production)
VITE_API_ROOT=/api
