# API base URL including /api suffix (no trailing slash after /api)
# Local (Vite proxy): /api
# Local (direct to API): http://localhost:5172/api
# Production: https://api.v2.cimmple.net/api
# If omitted on a hosted hostname, the app uses the production API automatically.
VITE_API_ROOT=/api
