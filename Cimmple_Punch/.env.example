# API base URL including /api suffix (no trailing slash)
# Local: http://localhost:5172/api
# Production build: https://api.v2.cimmple.net/api
# If omitted, localhost uses the local API and punch.cimmple.net uses production.
VITE_API_ROOT=http://localhost:5172/api
